pre-commit:
  parallel: true
  commands:
    typecheck:
      run: npx tsgo --noEmit
    lint:
      run: npx biome check --diagnostic-level=error src/
    test:
      run: npx vitest run
    cpd:
      run: npx jscpd src/ --threshold 0.5 --mode weak
